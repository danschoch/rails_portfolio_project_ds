<%# TODO: refactor two employee forms to one. Use partials with "local:"" option for local variables %>

<%= form_for @employee do |f| %>
    First Name: <%= f.text_field :first_name %>
    Last Name: <%= f.text_field :last_name %>
    <% if @curr_user.lead == 'true' %>
        Title: <%= f.text_field :title %>
    <% end %>
    <br>
    <br>
    Log In Information
    Email: <%= f.text_field :email %><br>
    <br>

    <%= f.submit "Submit"%>

    <%# link_to "Click here to change your password", #TODO: create view and path for editing password %>
    <%# Password: <%= f.password_field :password %>
    <%#Password Confirmation: <%= f.password_field :%>

<% end %>