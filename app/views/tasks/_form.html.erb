<%= form_for @task, url: url, method: method do |f| %>
    Select A Project for this Task: <%= f.collection_select(:project_id, Project.active_projects(session[:organization_id]), :id, :name) %> <%# TODO: delete this and make a hidden field with project_id if it doesn't already exist %>
    Title: <%= f.text_field :content %>
    Due Date: <%= f.date_select :due_date, default: Date.tomorrow %>
    Notes for Employees: <%= f.text_area :lead_notes %>

    <%= f.submit "Submit Task"%>
   
<% end %>